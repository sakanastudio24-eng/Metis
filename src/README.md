# Source Guide

`src/` contains the Chrome extension runtime code.

Main areas:

- `app/`: React panel state and UI
- `background/`: Manifest V3 service worker
- `content/`: content-script bootstrap and Shadow DOM mount
- `features/`: scan, detection, scoring, and insight layers
- `shared/`: shared helpers and shared type contracts
- `styles/`: Tailwind entry and design tokens
