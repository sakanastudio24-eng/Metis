# App Folder

This folder owns the injected React UI.

Key files:

- `App.tsx`: coordinates the scan loop and panel props
- `useMetisState.ts`: local panel and snapshot state
- `components/`: injected panel surfaces and design references
- `data/`, `lib/`, `types/`: legacy design support files that can be trimmed later if no longer needed
