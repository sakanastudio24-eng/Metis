# App Components Folder

This folder contains the panel UI components and design reference components.

Current focus:

- `PhaseOneShell.tsx`: the live injected panel used by the extension
- `MetisExtension.tsx`: a large design reference file that can be reduced later if it stops being useful
