# Background Folder

This folder contains the Manifest V3 service worker.

The worker is intentionally minimal in Phase 2 and only exists so future extension
events and privileged Chrome API work have a clean home.
