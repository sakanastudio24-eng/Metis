# Content Folder

This folder mounts Metis into the active webpage.

The content script creates a Shadow DOM host, injects the bundled styles, and mounts
the React app so the extension UI stays isolated from host page CSS.
