# Features Folder

This folder keeps the product pipeline separated by phase responsibility.

Subfolders:

- `scan/`: page data collection and cleanup
- `detection/`: issue generation from normalized scan metrics
- `scoring/`: score and severity logic
- `insights/`: final user-facing explanation and cost language
