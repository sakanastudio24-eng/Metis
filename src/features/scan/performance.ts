import type { ResourceSummary } from "../../shared/types/audit";

export function collectResourceSummaries(): ResourceSummary[] {
  throw new Error("collectResourceSummaries is reserved for Phase 2.");
}
