import type { PageContext } from "../../shared/types/audit";

export function parsePageContext(href: string): PageContext {
  void href;
  throw new Error("parsePageContext is reserved for Phase 2.");
}
