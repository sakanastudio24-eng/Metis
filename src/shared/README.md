# Shared Folder

This folder holds the contracts and helpers used across phases.

Use it for:

- shared type definitions
- storage helpers
- small cross-cutting utilities that should not live inside a single feature folder
